<div class="side_bar_header">
  <div class="user-name">
    <%= link_to current_user.name, edit_user_path(current_user) %>
  </div>
  <div class="create-btn"><%= link_to 'チャットを作成する', new_room_path %></div>
</div>

<div class="side_bar_room">
  <div class="room-list">
  <% current_user.rooms.each do |r| %>
    <div class="room-name"><%= link_to r.name, room_messages_path(r) %></a></div>
  <% end %>
  </div>
</div>